{{ form_row(form.name) }}

{# if admin
{{ form_row(form.beneficiaire) }}
#}

<div class="row">
    <div class="col-6">
        {{ form_row(form.startTime) }}
    </div>
    <div class="col-6">
        {{ form_row(form.endTime) }}
    </div>
</div>

<div class="row">
    <div class="col-6">
        {{ form_row(form.area) }}
    </div>
    <div class="col-6">
        {{ form_row(form.room) }}
    </div>
</div>

<div class="row">
    <div class="col-6">
        {{ form_row(form.description) }}
    </div>
    <div class="col-6">
        {{ form_row(form.type) }}
    </div>
</div>