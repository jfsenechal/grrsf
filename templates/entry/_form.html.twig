{{ form_start(form) }}


<div class="row">
    <div class="col-6">
        {% include 'entry/_form_entry.html.twig' %}
    </div>
    <div class="col-6">
        {% include 'entry/_form_periodicity.html.twig' %}
    </div>
</div>



<button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
