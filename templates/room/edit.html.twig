{% extends 'layout_front.html.twig' %}

{% block title %}{{ room.name }}{% endblock %}

{% block body %}
    <h1>{{ room.name }}</h1>

    {{ include('room/_form.html.twig', {'button_label': 'form.btn.update'}) }}

    <br />
    <a href="{{ path('grr_area_show',{'id': room.area.id}) }}"><i class="fas fa-arrow-left"></i> {{ 'form.btn.cancel'|trans({}, 'actions') }}</a>

    {{ include('room/_delete_form.html.twig') }}
{% endblock %}
