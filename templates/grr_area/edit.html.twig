{% extends 'base.html.twig' %}

{% block title %}Edit GrrArea{% endblock %}

{% block body %}
    <h1>{{ grr_area.areaName }}</h1>

    {{ include('grr_area/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('grr_area_index') }}">{{ 'back.to.list'|trans({}, 'actions') }}</a>

    {{ include('grr_area/_delete_form.html.twig') }}
{% endblock %}
