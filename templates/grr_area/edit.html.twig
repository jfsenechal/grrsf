{% extends 'base.html.twig' %}

{% block title %}Edit GrrArea{% endblock %}

{% block body %}
    <h1>{{ grr_area.areaName }}</h1>

    {{ include('grr_area/_form.html.twig', {'button_label': 'form.btn.update'}) }}

    <br/>
    <a href="{{ path('grr_area_show',{'id':grre_area.id}) }}"><i
                class="fas fa-arrow-left"></i> {{ 'form.btn.cancel'|trans({}, 'actions') }}</a>

    {{ include('grr_area/_delete_form.html.twig') }}
{% endblock %}
