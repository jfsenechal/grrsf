{% extends 'base.html.twig' %}

{% block title %}Edit GrrEntry{% endblock %}

{% block body %}
    <h1>{{ grr_entry.name }}</h1>

    {{ include('grr_entry/_form.html.twig', {'button_label': 'Update'}) }}

    <br/>
    <a href="{{ path('grr_entry_show',{'id':grr_entry.id}) }}"><i
                class="fas fa-arrow-left"></i> {{ 'form.btn.cancel'|trans({}, 'actions') }}</a>

    {{ include('grr_entry/_delete_form.html.twig') }}
{% endblock %}
