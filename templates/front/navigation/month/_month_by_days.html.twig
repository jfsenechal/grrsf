<table class="table table-bordered">
    <thead>
    <tr>
        <th scope="col" class="text-center" colspan="8">
            <a href="{{ grrGenerateRouteMonthView() }}">
                {{ firstDay.monthName|upper }} {{ firstDay.year }}
            </a>
        </th>
    </tr>
    <tr>
        <th>{{ 'week'|trans }}</th>
        {% for dayName in listDays %}
            <th scope="col">{{ dayName|trans }}</th>
        {% endfor %}
    </tr>
    </thead>
    <tr class="">

        {% for day in days %}

        {% if loop.first %}
            {% include '@grr_front/navigation/month/_cell_week.html.twig' with {'numericWeek': day.week } %}
            {{ grrCompleteTr(day, 'begin') }}
        {% endif %}

        {% include '@grr_front/navigation/month/_cell_day.html.twig' %}
        {% if day.isSunday %}

    </tr>
    <tr class="">
        {% include '@grr_front/navigation/month/_cell_week.html.twig' with {'numericWeek': firstDay.week } %}

        {% endif %}

        {% if loop.last %}
            {{ grrCompleteTr(day, 'end') }}
        {% endif %}

        {% endfor %}
</table>