{% extends 'layout_front.html.twig' %}

{% block title %}GrrArea index{% endblock %}

{% block body %}

    {% include 'calendar/navigation/_navigation.html.twig' %}

    <h3 class="text-center text-success">
        {{ 'week'|trans }} du {{ week.firstDay|date('d-m') }}
        au {{ week.lastDay|date('d-m') }}
    </h3>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th>{{ 'ressources'|trans }}</th>
            {% for day in week.days %}
                <th>{{ day|date('l') |trans|lower }} {{ day|date('d') }} {{ day|date('F')|trans|lower }}</th>
            {% endfor %}
        </tr>
        </thead>
        <tbody>
        {% for room in rooms %}
            <tr>
                <td>{{ room.name }}</td>
                {% for day in week.data %}
                    <th>
                        {% for entry in day.entries %}
                            {{ entry.name }}
                        {% endfor %}
                    </th>
                {% endfor %}
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

{% block scripts %}
    {% include 'front/_menu_select.js.html.twig' %}
{% endblock %}