<table class="table">
    <tr>
        <th colspan="3"><a href="{{ path('grr_front_home', {'month': previous|date('n')}) }}">Précédent</a></th>
        <th colspan="2">{{ current | date('Y-m') }}</th>
        <th colspan="3"><a href="{{ path('grr_front_home', {'month': next|date('n')}) }}">Suivant</a></th>
    </tr>
    <tr>
        <th>{{ 'week'|trans }}</th>
        <th>{{ 'monday'|trans }}</th>
        <th>{{ 'tuesday'|trans }}</th>
        <th>{{ 'wednesday'|trans }}</th>
        <th>{{ 'thursday'|trans }}</th>
        <th>{{ 'friday'|trans }}</th>
        <th>{{ 'saturday'|trans }}</th>
        <th>{{ 'sunday'|trans }}</th>
    </tr>
    <tbody>
    <tr>
        {% for day in days %}
            {% include 'default/_week_line.html.twig' %}
        {% endfor %}
    <tr>
    </tbody>
</table>
