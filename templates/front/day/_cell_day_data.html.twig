{% if position == 0 %}
    <td rowspan="{{ entry.cellules }}" class="table-info">{{ entry.name }}</td>
{% else %}
    <td>
    <a href="{{ generateRouteAddEntry(area.id,room.id, day.day) }}" class="float-right" title="{{ 'btn.entry.add.title'|trans }}"> <i
                class="fas fa-plus"></i> </a>
    </td>
{% endif %}