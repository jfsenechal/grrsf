<form method="post" action="{{ path('grr_utilisateurs_delete', {'login': grr_utilisateur.login}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grr_utilisateur.login) }}">
    <button class="btn">Delete</button>
</form>
