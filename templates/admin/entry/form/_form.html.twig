{{ form_start(form) }}


<div class="row">
    <div class="col-6">
        {% include '@grr_admin/entry/form/_form_entry.html.twig' %}
    </div>
    <div class="col-6">
        {% include '@grr_admin/entry/form/_form_periodicity.html.twig' %}
    </div>
</div>

<input class="btn btn-primary" type="submit" name="submit">{{ button_label|default('Save') }}</input>
{{ form_end(form) }}
