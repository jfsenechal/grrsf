{% extends 'base.html.twig' %}

{% block title %}GrrSetting{% endblock %}

{% block body %}
    <h1>GrrSetting</h1>

    <td>

        <a href="{{ path('grr_setting_edit', {'id': grr_setting.id}) }}">edit</a>
    </td>

    <table class="table">
        <tbody>
        <tr>
            <th>Name</th>
            <td>{{ grr_setting.name }}</td>
        </tr>
        <tr>
            <th>Value</th>
            <td>{{ grr_setting.value }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('grr_setting_index') }}">back to list</a>

    <a href="{{ path('grr_setting_edit', {'id': grr_setting.id}) }}">edit</a>

    {{ include('grr_setting/_delete_form.html.twig') }}
{% endblock %}
