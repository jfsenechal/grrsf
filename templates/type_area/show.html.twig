{% extends 'layout_front.html.twig' %}

{% block title %}{{ type_area.typeName }}{% endblock %}

{% block body %}

    <h1>{{ type_area.typeName }}</h1>

    {% include 'type_area/_actions.html.twig' %}

    <table class="table table-bordered">
        <tbody>
        <tr>
            <th>{{ 'typeArea.form.orderDisplay.label'|trans }}</th>
            <td>{{ type_area.orderDisplay }}</td>
        </tr>
        <tr>
            <th>{{ 'typeArea.form.couleur.label'|trans }}</th>
            <td style="background-color: {{ type_area.couleur }}">{{ type_area.couleur }}</td>
        </tr>
        <tr>
            <th>{{ 'typeArea.form.typeLetter.label'|trans }}</th>
            <td>{{ type_area.typeLetter }}</td>
        </tr>
        <tr>
            <th>{{ 'typeArea.form.disponible.label'|trans }}Disponible</th>
            <td>{{ type_area.disponible }}</td>
        </tr>
        </tbody>
    </table>

    {{ include('type_area/_delete_form.html.twig') }}
{% endblock %}
