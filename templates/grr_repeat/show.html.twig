{% extends 'base.html.twig' %}

{% block title %}GrrRepeat{% endblock %}

{% block body %}
    <h1>GrrRepeat</h1>

    <a href="{{ path('grr_repeat_edit', {'id': grr_repeat.id}) }}">edit</a>

    <table class="table table-bordered">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ grr_repeat.id }}</td>
        </tr>
        <tr>
            <th>StartTime</th>
            <td>{{ grr_repeat.startTime }}</td>
        </tr>
        <tr>
            <th>EndTime</th>
            <td>{{ grr_repeat.endTime }}</td>
        </tr>
        <tr>
            <th>RepType</th>
            <td>{{ grr_repeat.repType }}</td>
        </tr>
        <tr>
            <th>EndDate</th>
            <td>{{ grr_repeat.endDate }}</td>
        </tr>
        <tr>
            <th>RepOpt</th>
            <td>{{ grr_repeat.repOpt }}</td>
        </tr>
        <tr>
            <th>RoomId</th>
            <td>{{ grr_repeat.roomId }}</td>
        </tr>
        <tr>
            <th>Timestamp</th>
            <td>{{ grr_repeat.timestamp ? grr_repeat.timestamp|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
        <tr>
            <th>CreateBy</th>
            <td>{{ grr_repeat.createBy }}</td>
        </tr>
        <tr>
            <th>BeneficiaireExt</th>
            <td>{{ grr_repeat.beneficiaireExt }}</td>
        </tr>
        <tr>
            <th>Beneficiaire</th>
            <td>{{ grr_repeat.beneficiaire }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ grr_repeat.name }}</td>
        </tr>
        <tr>
            <th>Type</th>
            <td>{{ grr_repeat.type }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ grr_repeat.description }}</td>
        </tr>
        <tr>
            <th>RepNumWeeks</th>
            <td>{{ grr_repeat.repNumWeeks ? 'Yes' : 'No' }}</td>
        </tr>
        <tr>
            <th>OverloadDesc</th>
            <td>{{ grr_repeat.overloadDesc }}</td>
        </tr>
        <tr>
            <th>Jours</th>
            <td>{{ grr_repeat.jours ? 'Yes' : 'No' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('grr_repeat_index') }}">back to list</a>

    <a href="{{ path('grr_repeat_edit', {'id': grr_repeat.id}) }}">edit</a>

    {{ include('grr_repeat/_delete_form.html.twig') }}
{% endblock %}
