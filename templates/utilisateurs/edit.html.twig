{% extends 'layout_front.html.twig' %}

{% block title %}{{ utilisateur.email }}{% endblock %}

{% block body %}
    <h1>{{ utilisateur.email }}</h1>

    {{ include('utilisateurs/_form.html.twig', {'button_label': 'form.btn.update'}) }}

    <br />
    <a href="{{ path('grr_user_index') }}"><i class="fas fa-arrow-left"></i> {{ 'form.btn.cancel'|trans({},'actions') }}</a>

    {{ include('utilisateurs/_delete_form.html.twig') }}
{% endblock %}
